/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {UserConfig} from './user-config';

describe('UserConfig', () => {
  it('should create an instance', () => {
    expect(new UserConfig()).toBeTruthy();
  });
});
