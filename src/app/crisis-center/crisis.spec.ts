/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {Crisis} from './crisis';

describe('Crisis', () => {
  it('should create an instance', () => {
    expect(new Crisis()).toBeTruthy();
  });
});
